Git is a "snapshot" system. Think of Git as a camera that takes photos of your entire project at moments you choose. Each photo is called a commit. Git stores these photos in a hidden `.git/` folder inside your project.

# Get Started

# Branching
